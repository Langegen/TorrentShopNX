#ifndef PC_NETINET_TCP_COMPAT_H
#define PC_NETINET_TCP_COMPAT_H

#include "../sys/socket.h"

// TCP_NODELAY is defined in sys/socket.h above; nothing else from
// netinet/tcp.h is used by the engine.

#endif
